# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-auto-build' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for ios-auto-build

  target 'ios-auto-buildTests' do
    inherit! :search_paths
     pod 'SDWebImage', :inhibit_warnings => true
    pod 'MJRefresh', :inhibit_warnings => true
    pod 'AFNetworking', :inhibit_warnings => true
    pod 'SDAutoLayout', :inhibit_warnings => true
    pod 'YYModel', :inhibit_warnings => true
  end

  target 'ios-auto-buildUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
